# hinet-rewrite-domains

Single source of truth for the domain list used by:
- gomami AdGuardHome DNS rewrites
- hinet sniproxy SNI table

## File
- `domains.txt`: one domain per line (lowercase), no wildcards.

## Notes
This repo intentionally contains only domain names (no IPs, no keys).
